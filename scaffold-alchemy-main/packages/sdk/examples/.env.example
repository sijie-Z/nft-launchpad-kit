# Agent SDK demo environment (copy to .env and fill in)
# Local demo defaults (Hardhat chain + local platform server):
#   BASE_URL=http://127.0.0.1:56900
#   RPC_URL=http://127.0.0.1:8545
#   AGENT_PRIVATE_KEY=<hex, no defaults for real use>
#   USER_PRIVATE_KEY=<hex>
#   NLA_API_KEY=<platform PLATFORM_API_KEY, if the server enforces it>

# Sepolia demo (after #15 deploy):
#   BASE_URL=https://your-instance.example.com
#   RPC_URL=https://eth-sepolia.g.alchemy.com/v2/<key>
#   CHAIN=sepolia  (not used yet — the demo pins "localhost"; change in the script)
